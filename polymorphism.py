# Polymorphishm

# Poly = Many, Morph = Form; Polymorphishm means "One interface, Many implementation"
# The same method, but different objects perform different actions
# If a method in a parent class is overridden with different business logic in its different child class, the base class method is polymorphic method. 
